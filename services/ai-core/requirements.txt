# RansomEye v1.0 AI Core (Phase 6 - Read-Only, Non-Blocking)
# Python 3.10+ only

# PostgreSQL database client
psycopg2-binary==2.9.9

# UUID generation (for model_version_id, cluster_id)
uuid==1.30

# Feature extraction and clustering (scikit-learn)
scikit-learn==1.3.2
numpy==1.24.3

# SHAP explainability (minimal SHAP-like explanations for Phase 6)
# Note: Full SHAP library not used in Phase 6 minimal, simple explanation method used instead
# shap==0.43.0  # Not required for Phase 6 minimal implementation

# Environment variable validation
pydantic==2.5.0
pydantic-settings==2.1.0

# Date/time parsing
python-dateutil==2.8.2
