{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RansomEye UBA Delta Summary",
  "description": "Frozen schema for delta summaries. All fields are mandatory. Zero optional fields allowed.",
  "type": "object",
  "required": [
    "summary_id",
    "identity_id",
    "baseline_hash",
    "observation_window_start",
    "observation_window_end",
    "total_deltas",
    "delta_types_present",
    "summary_hash",
    "immutable_hash",
    "ledger_entry_id"
  ],
  "additionalProperties": false,
  "properties": {
    "summary_id": {
      "type": "string",
      "format": "uuid",
      "description": "Unique identifier for this summary (UUID v4)"
    },
    "identity_id": {
      "type": "string",
      "format": "uuid",
      "description": "Identity identifier"
    },
    "baseline_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "SHA256 hash of baseline used for comparison"
    },
    "observation_window_start": {
      "type": "string",
      "format": "date-time",
      "description": "RFC3339 UTC timestamp of observation window start"
    },
    "observation_window_end": {
      "type": "string",
      "format": "date-time",
      "description": "RFC3339 UTC timestamp of observation window end"
    },
    "total_deltas": {
      "type": "integer",
      "minimum": 0,
      "description": "Total number of deltas in observation window"
    },
    "delta_types_present": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "NEW_EVENT_TYPE",
          "NEW_HOST",
          "NEW_TIME_BUCKET",
          "NEW_PRIVILEGE",
          "FREQUENCY_SHIFT"
        ]
      },
      "description": "Delta types present in observation window (explicit enumeration)"
    },
    "summary_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "SHA256 hash of summary content (for validation)"
    },
    "immutable_hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "SHA256 hash of summary record (immutability guarantee)"
    },
    "ledger_entry_id": {
      "type": "string",
      "format": "uuid",
      "description": "Audit ledger entry ID for this summary"
    }
  }
}
